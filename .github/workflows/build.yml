name: Build ZMK firmware

on:
  workflow_dispatch:
  push:
    paths:
      - config/**
      - build.yaml
  pull_request:
    paths:
      - config/**
      - build.yaml

permissions:
  contents: read

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
